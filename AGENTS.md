# Commit instructions
- One-line commit messages; no multiline description unless a single line is genuinely insufficient
- No `Co-Authored-By` trailer
- Put changed tests in the same commit as the changed feature
- Make well-scoped commits
